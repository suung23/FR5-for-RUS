# 실제로 쓰는 것만 적는다.
#
# pandas 와 seaborn 은 쓰지 않는다. 이 도구가 다루는 표는 수백 행짜리 평면 CSV 이고,
# 표준 csv 모듈과 numpy 로 충분하다. 실험실 장비에 무거운 의존성을 새로 들이는 것은
# 그 자체가 부작용이며, 분석을 돌리려다 설치에 막히는 편이 더 나쁘다.
numpy>=1.24
scipy>=1.10
matplotlib>=3.6
