# 팬텀 힘 유지 실험 프로토콜 — 8개 대역 × 500 mL 교란 3회

2026-09-03 작성. 이 실험이 검증하는 것은 두 가지다.

1. **미소 지령 실행 수정.** 2026-09-03 이전에는 서보의 idle 문턱
   (`IDLE_VEL_EPS_RAD_S = 1e-3 rad/s`)이 힘 조절기의 수렴 지령을 전부 "정지 의도"
   로 버렸다 — A_t0p5_r1 실측에서 조절기가 18 초 동안 전진을 지령하는데 팁이
   2 µm 만 움직였고, 힘이 목표 아래 0.1~0.3 N 에서 영원히 정체했다. 수정 후에는
   접촉 프로빙 중 서보가 의도를 추측하지 않는다 (`probing_mode` 구독 →
   `JointCommandLimiter.step(..., explicit_intent=True)`). 이 실험이 그 수정의
   첫 팬텀 검증이다.
2. **대역별 유지·교란 응답.** 0.5 ~ 4.0 N 여덟 대역에서 500 mL 교란(주입/회수
   각 3회)에 대한 힘 응답과 **로봇 변위**를 함께 기록한다. 힘이 돌아온 것이
   로봇이 받아 낸 것인지 팬텀이 스스로 누운 것인지는 변위 없이는 못 가른다.

| 항목 | 값 |
|---|---|
| 목표 대역 | 0.5, 1.0, 1.5, 2.0, 2.5, 3.0, 3.5, 4.0 N |
| 교란 | 500 mL 주입 3회 + 회수 3회, 대역마다 |
| 유지 구간 | 대역마다 30 s (교란 전, 정착 기준선) |
| 진입 문턱 | 목표 − 0.1 N (스크립트가 자동 설정) |
| 이탈 문턱 / 밴드 | 0.10 N / 0.05 N (스크립트가 자동 설정) |
| 캡처 위치 | `runs_sweep500/` (예전 실행과 분리) |

## 0. 전제 조건 (실험 전날까지)

수정 코드가 로봇 PC에 배포·빌드되어 있어야 한다:

```bash
cd ~/FR5-for-RUS
git pull
python3 -m pytest fr5_control/fr5_control/test/test_joint_command_limiter.py -q
#   → 15 passed 확인 (미소 지령 실행 시험 2개 포함)
colcon build --packages-select fr5_control
source install/setup.bash
```

중력 교정 프로파일이 없거나 의심스러우면 먼저 `./scripts/start_session.sh --calib`
로 전자영점 + 다자세 중력 식별을 마친다. 교정이 무효면 접촉 판정 자체가 보류되어
힘 제어가 열리지 않는다.

## 1. 세션 기동 — 터미널 1

```bash
cd ~/FR5-for-RUS
./scripts/start_session.sh
```

기동 로그에서 확인:

- 브리지: `PX6D 스트리밍 시작`, 교정 유효
- `us_diff_ik`: `힘 유지: 목표 3.0 ± 0.1 N ...` (probe.yaml 배포값이 찍히는지)

그다음 **시험 자세에서, 프로브가 아무것도 안 닿은 채로 GUI 의 Zero(작동 영점)**
를 누른다. 무접촉 잔차(평균 0.34 N)가 0.5~1.5 N 대역과 같은 크기라, 이 영점
없이는 저대역 결과가 무의미하다.

## 2. 실시간 감시 — 터미널 2

```bash
cd ~/FR5-for-RUS && source install/setup.bash
python3 force_hold_validation/monitor_force.py
```

"지령이 없다 / 지령은 있는데 안 간다 / 가는데 느리다" 를 이 화면이 가른다.
수정이 제대로 들어갔다면 **"지령은 있는데 안 간다" 가 더 이상 나오면 안 된다.**

## 3. 실험 본체 — 터미널 3

```bash
cd ~/FR5-for-RUS/force_hold_validation && source ../install/setup.bash
./run_repeat.sh 1 --targets "0.5 1.0 1.5 2.0 2.5 3.0 3.5 4.0" \
                  --step-ml 500 --out-dir runs_sweep500
```

스크립트가 대역마다 진입 문턱(목표−0.1)·이탈(0.10)·밴드(0.05)·목표를 스스로
걸고, 힘이 앉기를 기다렸다가 유지 30 s → 교란 캡처 순으로 진행한다.

### 조작자가 할 일

1. 첫 대역에서 "접촉 대기" 가 뜨면 스타일러스로 프로브를 팬텀에 누른다.
   진입하면 로봇이 z 를 잡는다. **이후 세션 끝까지 프로브를 떼지 않는다.**
2. 교란 캡처마다 **500 mL 주입 3회 + 회수 3회**:

   | 키 | 뜻 |
   |---|---|
   | `i` | 주입 시작 순간 |
   | `w` | 회수 시작 순간 |
   | `s` | 표면 안정됨 |
   | `u` | 마지막 표시 취소 |
   | `q` | 캡처 종료 (6회 끝난 뒤) |

3. 대역 전환·정착 대기는 스크립트가 한다. 조작자는 주사기와 키만.

### 수정 검증 포인트 (첫 대역에서 바로 확인)

- 서보 로그(터미널 1)에 **"접촉 프로빙 진입 — 미소 관절 지령을 그대로 실행한다"**
  가 찍힌다.
- 힘이 목표에 **도달**한다 — 0.5 N 대역에서 0.50 ± 0.05 N (수정 전엔 0.41 정체).
- 유지 30 s 동안 팬텀 이완을 쫓는다 — 힘이 흘러내리지 않는다 (수정 전엔
  0.49 → 0.38 N 하락).
- **불합격 모드 — 목표 주위에서 떨면(hunting):** 강성 대비 게인 문제다. 순서대로:

  ```bash
  ros2 param set /us_diff_ik_node contact_control.deadband_n 0.15        # 1차
  ros2 param set /us_diff_ik_node contact_control.admittance_b_z 2000.0  # 2차
  ```

  값을 바꿨으면 보고서에 적는다 — 캡처 메타가 라이브 값을 읽으므로 기록은 남는다.

### 예상되는 두 가지 — 사고 아님

- **0.5 N + 500 mL 회수**: 힘이 이탈 문턱(0.10 N) 아래로 떨어져 접근 모드로
  복귀할 수 있다. 스크립트가 멈추며 이어서 돌릴 명령을 그대로 출력한다 —
  다시 누르고 그 명령을 붙여넣는다. 이것 자체가 저대역의 소견이다.
- **4.0 N + 500 mL 주입**: 피크가 경고(4.5 N)를 넘고 한계(5.0 N)에 근접할 수
  있다. 경고 이상 전진 금지·5 N 강제 후퇴가 개입하는 것이 정상 동작이고, 전부
  기록되어 안전 표에 남는다. 조작자는 놀라지 말 것.

## 4. 분석 — 표와 플롯

```bash
cd ~/FR5-for-RUS/force_hold_validation
python3 run_force_hold_analysis.py --runs runs_sweep500 --output-dir outputs_sweep500
```

### 나오는 표

| 파일 | 내용 |
|---|---|
| `disturbance_events.csv` | 대역 × 방향 × 3회 이벤트마다 피크 오차 · 복귀 시간 · **로봇 순변위 `travel_mm` · 피크 변위 `peak_travel_mm`** |
| `hold_by_target.csv` | 대역별 SD · 정착점 대비 오차 · **유지 중 이동거리 `travel_mm`** |
| `safety_margin.csv` | 실행별 최악 힘과 경고·한계 여유 |
| `regulation_evidence.csv` | 이벤트별 흡수량·반사실 힘 (강성 필요) |
| `analysis_report.md` | 위 전부 + 제외 실행과 사유 |

### 나오는 플롯

| 그림 | 내용 |
|---|---|
| `fig1_force_traces` | 대역별 힘(실선) + **로봇 변위(점선)** 시계열, 밴드·한계 표시 |
| `fig3_disturbance` | 교란 정렬 2×2 — 위 행 힘 오차, **아래 행 로봇 변위**, 같은 시간축 |
| `fig2_hold_quality` | 대역별 유지 품질 (오차 · RMSE · 밴드 내 체류율) |
| `fig4_safety_margin` | 실행별 최악 힘 vs 경고·한계 |
| `fig5_regulation_evidence` | 힘 평평 + 팔 이동 = 흡수 증거 |
| `fig6_excursion_recovery` | 밴드 초과 노출 시간 |

### 판독 요령

- **fig3**: 힘이 돌아오는 동안 변위가 계단으로 움직이면 **로봇이 받아 낸 것**,
  변위가 평평한데 힘만 돌아오면 팬텀이 스스로 누운 것이다.
- **hold 의 `travel_mm`**: 힘이 잡혀 있는데 이 값이 0 이면 지령이 실행되지 않은
  것이다 — 2026-09-03 버그가 정확히 이 모양으로 숨어 있었다.
- **대역 간 비교는 SD 로.** 목표 대비 평균 편차는 데드밴드가 설계대로 낸 것이
  섞여 있다.

## 5. 원복

`param set` 은 런타임 전용이다. **터미널 1 을 Ctrl-C 로 내리고 다시 띄우면
probe.yaml 배포값(진입 2.0 / 이탈 0.3 / 밴드 0.05 / 목표 3.0)으로 돌아간다.**
별도 되돌리기 작업이 없다.

## 안전 메모

접촉 프로빙 중에는 데드맨을 놓아도 힘 유지가 계속되는 것이 설계다. 급히 빼야
하면:

1. 주사기 회수로 힘을 이탈 문턱 아래로 → 접근 모드 복귀 → 데드맨 해제
   (워치독 후퇴 −z, 5 mm/s)
2. 그래도 안 되면 티치펜던트 **E-stop**

힘 한계 5 N(‖F‖)은 팬텀에서 도달 가능한 값으로 잡혀 있고, 초과 시 강제 후퇴
(5 mm/s)가 조절기 안에서 걸린다. 이 층은 교정·모드와 무관하게 산다.
